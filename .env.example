# Path to the vault checkout on the host (mounted into the container)
VAULT_PATH=/path/to/vault-checkout

# Bearer token for internal/Tailscale access (Desktop, Code) — min. 32 random bytes
TOKEN_INTERNAL=changeme-generate-with-openssl-rand-hex-32

# Bearer token for public/NPM access (Mobile, Web) — min. 32 random bytes, different from TOKEN_INTERNAL
TOKEN_EXTERNAL=changeme-generate-with-openssl-rand-hex-32

# Public domain this server is reachable under (used for MCP resource/audience checks later)
DOMAIN=obsidian-mcp.example.com

# Port the server listens on inside the container
PORT=3000
